function createSlider(placeholder, min, max, pos, fxn) {
  const placeholder_container = document.createElement("label");
  placeholder_container.classList.add("config-label");
  placeholder_container.innerText = placeholder;
}
createSlider("Vignitte Spread");
