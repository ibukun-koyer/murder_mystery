function clear(obj) {
  if (object) {
    use(1, "isVisible", "isNotVisible", interaction);
  }
  use(2, "isEnabled", "isNotEnabled", obj);
}
$addEventListener(times, "click", () => {
  gamePage.innerHTML = "";
  clear(game);
});
$addEventListener(times2, "click", () => {
  clear(journal_page);
});
